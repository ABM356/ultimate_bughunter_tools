# PLACEHOLDER ONLY. Replace with values from a real secret manager
# (External Secrets Operator + AWS Secrets Manager / GCP Secret Manager / Vault).
# Never commit real values to git.
apiVersion: v1
kind: Secret
metadata:
  name: hopeup-secrets
  namespace: hopeup
  labels:
    app: hopeup
    tier: shared
type: Opaque
stringData:
  # Placeholders. Replace before deploy.
  POSTGRES_USER: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME_STRONG_PASSWORD"
  JWT_SECRET: "REPLACE_ME_64_BYTE_HEX"
  ANTHROPIC_API_KEY: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  STRIPE_SECRET_KEY: "REPLACE_ME"
  STRIPE_WEBHOOK_SECRET: "REPLACE_ME"
  ELASTICSEARCH_USERNAME: "elastic"
  ELASTICSEARCH_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  SENTRY_DSN: "REPLACE_ME"
