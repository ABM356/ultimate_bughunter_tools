apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: hopeup
  labels:
    app: frontend
    tier: web
spec:
  type: ClusterIP
  selector:
    app: frontend
    tier: web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
